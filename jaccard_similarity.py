#This function will take two sets and return both Jaccard distance and similarity. Make sure that the two inputs are sets and not lists or other types of data structures
def jaccard_compute(set1, set2):
    intersection = set1.intersection(set2)
    union = set1.union(set2)
    intersection_count = count(intersection)
    return()
